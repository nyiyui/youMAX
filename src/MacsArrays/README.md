Run the program in the this directory. The 50+ entry testdata should load automatically.

To access the original testdata after doing destructive operations (e.g. `edit`), type `load` and then `testdata`.
